# Общий план

---

* импортирует pygame;
* вызывает pygame.init();
* создаёт окно, например 800 × 600;
* запускает цикл while;
* обрабатывает событие pygame.QUIT;
* каждый кадр очищает экран каким-нибудь цветом;
* обновляет изображение на экране;
* ограничивает FPS через pygame.time.Clock().

